# package(default_visibility=["//visibility:public"])
load("@rules_cc//cc:defs.bzl", "cc_binary")

# https://bazel.build/reference/be/c-cpp#cc_binary
cc_binary(
    name = "hello",
    srcs = glob(["main/cpp/*.cpp"]),
    copts = ["-std:c++17"],
    visibility = ["//visibility:public"],
)
